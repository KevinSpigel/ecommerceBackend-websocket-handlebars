<div>
  <h1>Hi, {{user.name}}! Nice to meet you!</h1>

  {{#if user.isAdmin}}
    {{#each list}}
    <div>
      <p>Product: {{this.item}}</p>
      <p>Price: {{this.price}}</p>
    </div>
    {{/each }}
  {{else}}
  <h2>You do not have permission to view the content</h2>
  {{/if}}
</div>